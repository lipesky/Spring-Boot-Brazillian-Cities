# Spring-Boot-Brazillian-Cities

An example of spring boot to fetch address information, demonstrating the use of caching, custom converters (in this case, csv format), exception handling, external api consumption, swagger, SOLID and unit tests.
